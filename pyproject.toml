[project]
name = "budget-tracking-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot==20.7",
    "pyyaml==6.0.1",
    "aiohttp==3.9.3",
]

[tool.uv]
dev-dependencies = []
